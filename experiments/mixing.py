total = 10

for r1 in range(total+1):
    # if total <= 0: break
    print(f'{r1, total = }')
    total -= 1
    # for r2 in range(total):
    #     if total <= 0: break
    #     total -= 1
    #     print(f'{r2, total = }')
